pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build stage ishlayapti'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage ishlayapti'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy stage ishlayapti'
            }
        }
    }
}